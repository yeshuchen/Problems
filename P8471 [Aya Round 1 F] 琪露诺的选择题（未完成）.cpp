#include<bits/stdc++.h>
using namespace std;
int T, n, a, e;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    cin >> T;
    while(T--)
    {
        string s;
        cin >> n >> a >> e;
        cin >> s;
        for(int i = 0; i < s.size(); i++)
            if(s[i] == 'A')
                cnt++;
        
    }
    return 0;
}