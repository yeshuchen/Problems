#include<bits/stdc++.h>
using namespace std;
int T, n, k;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	cin >> T;
	while(T--)
	{
		int ans = 0;
		cin >> n >> k;
		for(int i = 1; i <= n; i++)
		{
			int a;
			cin >> a;
		}
		cout << ans << '\n';
	}
	return 0;
}
